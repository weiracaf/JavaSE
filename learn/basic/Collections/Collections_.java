package yuan.learn.basic.Collections;
//这期讲Collections工具类，主要是操作集合（List,Set,Map）的类
//都是静态方法直接调就行
public class Collections_ {
	//常用方法1.reverse(List)：反转List元素中的排序
	//2.shuffle(List)：对List集合中的元素进行随机排序
	//3.sort(List):对元素自然顺序对指定List集合按升序排序
	//4.sort(List,Comparator):不用说了，这就是定制排序呗
	//5.swap(List,int i,int j):List的第i个元素和第j个元素互换
	
	//6.max(Collection):根据自然排序，返回集合中最大值
	//7.max(Collection,Comparator):按我们做的排序规则返回最大值
	//8.min(Collection):根据自然排序，返回集合中最小值
	//9.min(Collection,Comparator):按我们做的排序规则返回最小值
	//10.frequency(Collection,Object):返回指定集合中出现指定元素的个数
	
	//11.copy(List dest,List src):将src内容复制到dest中
	//12.replaceAll(List list,Object oldVal,Object newVal)
	//使用新值替换List集合中所有旧值
	

}
